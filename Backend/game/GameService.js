class GameService {

    /* Generate new game session and store in memory, return the session's id. */
    create() {

    }

    /* Search for an available session by session id and increment the player count. */
    join() {

    }

    /* Search for an available session by session id and decrement the player count.  */
    lose() {

    }
}